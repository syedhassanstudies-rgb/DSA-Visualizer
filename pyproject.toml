[project]
name = "dsa-visualizer"
version = "0.1.0"
description = "CLI based DSA visualizer"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "groq>=1.5.0",
    "python-dotenv>=1.2.2",
]
